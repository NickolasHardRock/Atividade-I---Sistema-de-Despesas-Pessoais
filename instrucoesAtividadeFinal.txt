Atividade Final
Data de conclusão 30 de junho de 2026 às 23:59
Backend
Instruções
#  Disciplina de Backend

## Objetivo
Desenvolver uma API RESTful para um sistema de controle de despesas utilizando Node.js, Express, Sequelize e MySQL seguindo o padrão MVC.

---

# Tecnologias Obrigatórias

- Node.js FEITO
- Express FEITO
- Sequelize FEITO
- MySQL FEITO
- JWT Já possuo codado mas ainda não implementado
- bcrypt Já possuo codado mas ainda não implementado
- dotenv (*)

---

# Arquitetura Esperada

```text
src/
 ├── controllers/ FEITO
 ├── views/ FEITO
 ├── models/ FEITO
 ├── routes/ (*) posso fazer
 ├── middlewares/ (*) FEITO
 ├── config/ FEITO
 ├── database/ FEITO
 └── app.js FEITO
```

---

# Requisitos Funcionais

## Autenticação

### Cadastro de Usuário FEITO
- Nome 
- Email
- Senha 

### Login FEITO
- JWT
- Senha criptografada com bcrypt

---

# Entidades

## Usuário FEITO
- id
- nome
- email
- senha
- createdAt
- updatedAt

## Categoria FEITO
- id
- nome
- descricao

## Despesa FEITO
- id
- descricao
- valor
- data
- status (`PENDENTE`, `PAGA`)
- categoriaId
- usuarioId

---

# Relacionamentos FEITO

- Um usuário possui várias despesas FEITO
- Uma categoria possui várias despesas FEITO
- Uma despesa pertence a um usuário FEITO
- Uma despesa pertence a uma categoria FEITO

---

# Rotas RESTful

## Auth FEITO

```http
POST /users
POST /auth/login
```

## Categorias FEITO

```http
GET /categories
GET /categories/:id
POST /categories
PUT /categories/:id
DELETE /categories/:id
```

## Despesas FEITO

```http
GET /expenses 
GET /expenses/:id 
POST /expenses 
PUT /expenses/:id 
DELETE /expenses/:id 
```

---

# Requisitos de Filtros

Implementar filtros por:

- Categoria FEITO
- Status FEITO
- Período
- Valor mínimo
- Valor máximo

Exemplo:

```http
GET /expenses?status=PAGA&categoria=1
```

---

# APIs Estatísticas Obrigatórias

## Total de Gastos

```http
GET /dashboard/total-expenses
```

### Resposta

```json
{
  "total": 3500.50
}
```

---

## Quantidade de Despesas

```http
GET /dashboard/expenses-count
```

### Resposta

```json
{
  "quantidade": 45
}
```

---

## Gastos por Categoria

```http
GET /dashboard/expenses-by-category
```

### Resposta

```json
[
  {
    "categoria": "Alimentação",
    "total": 1200
  },
  {
    "categoria": "Transporte",
    "total": 800
  }
]
```

---

# Requisitos Sequelize

Obrigatório:

- Migrations
- Seeders
- Associations
- Validações
- Query Filters

---

# Segurança

- JWT
- bcrypt
- dotenv (*)
- Middleware de autenticação
- Tratamento global de erros

---

# Entregáveis

- Repositório GitHub
- Collection Postman
- Swagger (*)
- README
- Script SQL/Migrations